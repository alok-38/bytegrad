const fruits = ["Papaya", "Pineapple", "Mango", "GRapes", "Watermelon"];
const lastItemIndex = fruits.length - 1;
const lastItem = fruits[lastItemIndex];
const firstFruit = fruits[0];
const actualFruit = lastItem;
console.table(firstFruit, actualFruit);
console.table(fruits);
